# Activité 2 — Bug + tests

Objectif :
1. Lancer les tests : `pytest -q`
2. Identifier le bug dans `prime.py`
3. Corriger `is_prime` pour faire passer tous les tests
4. Bonus : améliorer l'algorithme (tester jusqu'à `i*i <= n`)

Consigne "IA" possible :
> Propose une correction minimale + explique + propose 2 tests supplémentaires (cas limites).
